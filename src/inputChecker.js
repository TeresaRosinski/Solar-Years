export function checkAge(userAge) {
  // let wrongAge = "Please enter an age greater than 0 and less than 150";
  // if(age<0 || age>150) {
  //   return wrongAge;
  // } else {
  //   return true;
  // }
}